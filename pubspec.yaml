name: nexachat
description: NexaChat starter
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.0.0
  firebase_auth: ^4.0.0
  cloud_firestore: ^4.0.0
  firebase_storage: ^11.0.0
  flutter_local_notifications: ^9.0.0
  uuid: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
